// Auth service - now using Supabase Auth via AuthContext
// This file is kept for backward compatibility but most auth logic
// is now handled directly in AuthContext via Supabase.

export const authService = {
  // Legacy methods kept as stubs for any remaining references
};
