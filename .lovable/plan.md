

## Corrigir idioma do HTML para Português

### Problema
O `index.html` está com `lang="en"`, o que faz navegadores oferecerem tradução automática.

### Alteração
Trocar `<html lang="en">` por `<html lang="pt-BR">` no `index.html`.

